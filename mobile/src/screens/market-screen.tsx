import { Text, View } from "react-native"

/** PLACEHOLDER — replaced by the ported screen. */
export function MarketScreen() {
  return (
    <View>
      <Text>MarketScreen — port pending</Text>
    </View>
  )
}
