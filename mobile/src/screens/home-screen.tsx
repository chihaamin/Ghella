import { Text, View } from "react-native"

/** PLACEHOLDER — replaced by the ported screen. */
export function HomeScreen() {
  return (
    <View>
      <Text>HomeScreen — port pending</Text>
    </View>
  )
}
