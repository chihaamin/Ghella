import { Text, View } from "react-native"

/** PLACEHOLDER — replaced by the ported screen. */
export function OnboardScreen() {
  return (
    <View>
      <Text>OnboardScreen — port pending</Text>
    </View>
  )
}
