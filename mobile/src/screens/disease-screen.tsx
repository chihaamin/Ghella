import { Text, View } from "react-native"

/** PLACEHOLDER — replaced by the ported screen. */
export function DiseaseScreen() {
  return (
    <View>
      <Text>DiseaseScreen — port pending</Text>
    </View>
  )
}
