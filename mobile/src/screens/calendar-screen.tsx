import { Text, View } from "react-native"

/** PLACEHOLDER — replaced by the ported screen. */
export function CalendarScreen() {
  return (
    <View>
      <Text>CalendarScreen — port pending</Text>
    </View>
  )
}
