import { Text, View } from "react-native"

/** PLACEHOLDER — replaced by the ported screen. */
export function DecideScreen() {
  return (
    <View>
      <Text>DecideScreen — port pending</Text>
    </View>
  )
}
