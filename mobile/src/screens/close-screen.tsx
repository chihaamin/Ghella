import { Text, View } from "react-native"

/** PLACEHOLDER — replaced by the ported screen. */
export function CloseScreen() {
  return (
    <View>
      <Text>CloseScreen — port pending</Text>
    </View>
  )
}
